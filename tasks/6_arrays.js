let arr = [1, 4, 2, 8, 6, -3];
// создать новый массив, в котором будут все значения из массива arr, но в обратном порядке
let arr_new = [];


for (let i = arr.length - 1; i >= 0; i = i - 1) {
  arr_new.push(arr[i]);
}

console.log(5);